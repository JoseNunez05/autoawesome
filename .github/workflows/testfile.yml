name: test yaml
on: push

pool: 
  vmImage: ubuntu-latest

stages:
  - stage: test stage
    jobs:
    - job: job1
      steps:
      - name: test run
        uses: github-action@v5
      - script: echo "this is job 1!"
    - job: job2
      dependsOn: "J2"
      steps:
      - name: test run
        uses: github-action@v5
      - script: echo "this is job 2!"
